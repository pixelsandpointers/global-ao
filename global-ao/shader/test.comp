#version 460 core

layout (local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

layout(std430, binding=1) buffer ssbo_vertices{
    vec3 vertex_pos[];
};

layout(std430, binding=2) buffer ssbo_triangles{
    uvec3 triangles[];
};

layout(std430, binding=3) buffer ssbo_aoOutput{
    float aoOutput[];
};


void main(){
    aoOutput[0] = 0.0f;
}